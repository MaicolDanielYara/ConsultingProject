@model IEnumerable<ConsultingProject.Core.Domain.Profesiones>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_MenuAdmin.cshtml";
}

<h1>Contenido de Profesiones</h1>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Profesion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Profesion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdProfesion">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdProfesion">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdProfesion">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
