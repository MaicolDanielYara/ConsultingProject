@model IEnumerable<ConsultingProject.Core.Domain.EjeSeleccionado>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Eje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProyectoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Eje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProyectoNavigation.IdProyecto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEje">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEje">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEje">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
